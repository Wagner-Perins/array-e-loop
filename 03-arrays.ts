const listaFrutas: string[] = ['banana', 'abacaxi', 'maçã', 'pera', 'uva', 'manga'];

listaFrutas[listaFrutas.length] = 'abacate';

const carros: string[] = [];

carros[carros.length] = 'Opala';
carros[carros.length] = 'Skyline';
carros[carros.length] = 'Camaro SS';

console.log(carros);

const lista: number[] = [];
const listaBoolean: boolean[] = [];